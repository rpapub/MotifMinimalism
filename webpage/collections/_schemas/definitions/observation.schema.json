{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://rpapub.github.io/MotifMinimalism/schemas/definitions/observation.schema.json",
    "type": "object",
    "properties": {
        "id": {
            "type": "string",
            "description": "Unique identifier for referencing this observation"
        },
        "status": {
            "type": "string",
            "enum": [
                "pass",
                "fail",
                "error",
                "unknown"
            ]
        },
        "timestamp": {
            "type": "string",
            "format": "date-time"
        },
        "context": {
            "type": "string",
            "description": "What this observation refers to (e.g., deployment, project)."
        },
        "input_data": {
            "type": "object",
            "description": "Concrete input data for testing this project",
            "additionalProperties": true
        },
        "output_data_observed": {
            "type": "object",
            "description": "Expected output data when executed with input_data",
            "additionalProperties": true
        },
        "findings": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "message": {
                        "type": "string"
                    },
                    "severity": {
                        "type": "string",
                        "enum": [
                            "low",
                            "medium",
                            "high"
                        ]
                    }
                },
                "required": [
                    "message"
                ]
            }
        }
    },
    "required": [
        "status",
        "timestamp"
    ],
    "allOf": [
        {
            "if": {
                "properties": {
                    "status": {
                        "enum": [
                            "fail",
                            "error"
                        ]
                    }
                }
            },
            "then": {
                "required": [
                    "findings"
                ]
            }
        }
    ]
}